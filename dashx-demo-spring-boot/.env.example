DASHX_BASE_URL=
DASHX_PUBLIC_KEY=
DASHX_PRIVATE_KEY=
DASHX_TARGET_ENVIRONMENT=

# Optional: Connection and Timeout Configuration (all timeout values in milliseconds)
# Uncomment and adjust these values if you experience connection issues
# DASHX_CONNECTION_TIMEOUT=10000
# DASHX_RESPONSE_TIMEOUT=30000
# DASHX_MAX_CONNECTIONS=500
# DASHX_MAX_IDLE_TIME=20000
